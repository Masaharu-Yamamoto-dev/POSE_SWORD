# 2026-08-20 10:40:00 — PR #15 改修レビュー

## 1. 全体のテーマ

PR #15 は「Network Update #15」で、主に対戦同期と通信の安定化がテーマでした。

## 2. バグ修正に当たる変更

### 2-1. 同期の分離

- 分離前: 位置と状態が混ざっていた
- 分離後: 位置同期 / HP や SP / ダッシュ状態を別々に扱う

影響ファイル:
- [POSE_SWORD_Unity/Assets/NetworkManager.cs](POSE_SWORD_Unity/Assets/NetworkManager.cs)

### 2-2. Client 側補間追従

- 受信した座標をそのまま置くのではなく、Lerp で順に近づける
- 最初のジャンプやズレを抑える

### 2-3. 安全な null / 未定義チェック

- `data.hostSword?.hp` のような安全アクセス
- 画像がまだ来ていない場合の表示を追加

影響ファイル:
- [src/App.jsx](src/App.jsx)

### 2-4. WebGL build 更新

- React 側が ver2.6 の build を読むように変更
- Unity 側の更新とブラウザ側の参照が一致するようにした

## 3. 機能追加に当たる変更

### 3-1. 画像送信の分離

- 統計データを先に送る
- 画像を後で送る
- `EXCHANGE_SWORD` と `EXCHANGE_IMAGE` を分けた

### 3-2. 補間追従の導入

- より自然な動きで同期するようになった

### 3-3. ステージ切替

- 独楽モード／剣モードでステージ表示を切り替える

## 4. まとめ

PR #15 は「対戦同期の安定化」が本筋であり、その中に新しい UX や演出の改善が混ざっていました。

つまり、

- バグ修正: 通信のズレや落ち、描画崩れを防ぐ内容
- 機能追加: 分離同期、補間、ステージ切替、受信中表示

という整理が最も自然です。
